# 综合修复方案

## 🔍 当前问题分析

### 1. **包依赖问题**
```
An error occurred while resolving packages: 
Project has invalid dependencies: com.unity.xr.arsubsystems: 
Package [com.unity.xr.arsubsystems@5.1.0] cannot be found
```

**原因**: `arsubsystems` 包可能：
- 需要从 Unity Registry 下载
- 版本号不正确
- 依赖其他包

### 2. **脚本引用问题**
MobileARTemplateAssets 中的脚本严重依赖 XR Interaction Toolkit：
- `ARTemplateMenuManager.cs` - 菜单管理
- `GoalManager.cs` - 目标管理  
- `ARFeatheredPlaneMeshVisualizerCompanion.cs` - AR 平面可视化

## 🚀 修复方案

### 方案 A: 完整修复（推荐）
重新添加 XR Interaction Toolkit 及其所有依赖

### 方案 B: 最小化修复
禁用依赖 XR Interaction Toolkit 的脚本，保留基础功能

### 方案 C: 降级方案
使用更稳定的包版本组合

## 🔧 立即执行的操作

### 步骤 1: 修复 arsubsystems 包问题
```json
// 修改 manifest.json，确保包版本兼容
{
  "dependencies": {
    // AR Foundation 完整集合
    "com.unity.xr.arfoundation": "5.1.0",
    "com.unity.xr.arcore": "5.1.0", 
    "com.unity.xr.arkit": "5.1.0",
    "com.unity.xr.arsubsystems": "5.1.0",
    
    // XR Interaction Toolkit 及其依赖
    "com.unity.xr.interaction.toolkit": "2.5.2",  // 更稳定的版本
    "com.unity.xr.management": "4.4.0",
    "com.unity.xr.core-utils": "2.2.3", 
    "com.unity.inputsystem": "1.7.0",
    
    // 其他必要包...
  }
}
```

### 步骤 2: 处理脚本依赖
如果选择方案 B（最小化）：
```bash
# 禁用依赖 XR Interaction Toolkit 的脚本
mv Assets/MobileARTemplateAssets/Scripts/ Assets/MobileARTemplateAssets/Scripts_Disabled/

# 或重命名扩展名
find Assets/MobileARTemplateAssets -name "*.cs" -exec mv {} {}.disabled \;
```

### 步骤 3: 验证修复
1. 更新 manifest.json
2. 重新打开 Unity
3. 等待包导入
4. 检查编译状态

## 📊 预期结果

### 如果使用方案 A（完整修复）：
- ✅ 所有包正常导入
- ✅ MobileARTemplateAssets 脚本正常编译
- ✅ 完整 AR 交互功能可用
- ⚠️ 可能需要处理示例脚本错误

### 如果使用方案 B（最小化修复）：
- ✅ 包依赖问题解决
- ✅ 核心脚本正常编译  
- ✅ 基础 AR 功能可用
- ❌ 高级交互功能不可用
- ✅ 错误数量大幅减少

## ⚠️ 风险评估

### 方案 A 风险：
- 可能重新引入之前的编译错误
- 需要确保包版本完全兼容
- 可能需要处理示例文件夹问题

### 方案 B 风险：
- 可能丢失重要功能
- 需要验证核心功能是否正常
- 可能需要修改场景引用

## 🛠️ 执行修复

让我先尝试方案 A（完整修复），如果失败再回退到方案 B。